# Main experiment config: STL-10 (unlabeled for SSL, labeled splits for the linear probe).
dataset:
  name: stl10
  root: data
  image_size: 96

augmentation:
  crop_scale: [0.3, 1.0]
  jitter_strength: 0.4
  grayscale_p: 0.2
  blur_p: 0.5

loader:
  batch_size: 256
  num_workers: 4

seed: 0
